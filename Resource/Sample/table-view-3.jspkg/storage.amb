top: VBox {
    storage: Storage {
		name: "storage"
		tables: {
			table0: "root"
		}
    }
    quit_button: Button {
		title: "Quit"
		pressed: Event() %{
			let str = top.storage.table("table0").toString() ;
			console.print("table0 = " + str + "\n") ;
			leaveView(1) ;
        %}
    }
}

